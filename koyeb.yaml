name: thumbnail-caption-bot
build:
  type: docker
deploy:
  ports: []   # No public ports required
  env:
    - key: API_ID
      value: ${API_ID}
    - key: API_HASH
      value: ${API_HASH}
    - key: BOT_TOKEN
      value: ${BOT_TOKEN}
health_check:
  disabled: true
